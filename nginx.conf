worker_processes auto;

events {
    worker_connections 1024;
}

http {
    real_ip_header X-Forwarded-For;
    set_real_ip_from 0.0.0.0/0;

    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header Host $host;
    proxy_set_header X-Forwarded-Proto $scheme;

    server {
        listen 80;

        # ① 反代你的前端页面（华夏小课堂）
        location / {
            proxy_pass https://huaxia-tech.github.io;
            proxy_set_header Host huaxia-tech.github.io;
        }

        # ② 反代 Umami 的 script.js
        location /script.js {
            proxy_pass https://umami-production-08d5.up.railway.app/script.js;
            proxy_set_header Host umami-production-08d5.up.railway.app;
        }

        # ③ 反代 Umami 的 API
        location /api/ {
            proxy_pass https://umami-production-08d5.up.railway.app/api/;
            proxy_set_header Host umami-production-08d5.up.railway.app;
        }

        # ⑤ 反代你的访问记录后端 API
        location /api/records/create {
            proxy_pass https://huaxia-classroom.vercel.app/api/records/create;
            proxy_set_header Host huaxia-classroom.vercel.app;
        }

        # ④ 反代 Umami 的后台页面
        location /websites/ {
            proxy_pass https://umami-production-08d5.up.railway.app/websites/;
            proxy_set_header Host umami-production-08d5.up.railway.app;
        }

        location /dashboard {
            proxy_pass https://umami-production-08d5.up.railway.app/dashboard;
            proxy_set_header Host umami-production-08d5.up.railway.app;
        }
    }
}

